// Legacy Prisma helper — intentionally removed during MongoDB migration.
// Some modules still import from this file; they must be migrated to MongoDB services.
// Keeping a minimal export surface avoids runtime crashes while migration is in progress.

module.exports = {};
